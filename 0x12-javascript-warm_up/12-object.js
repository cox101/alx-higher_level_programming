#!/usr/bin/node

// Define an object named myObject
const myObject = {
  type: 'object',
  value: 12
};

// Print the initial state of myObject
console.log(myObject);

// Modify the value property of myObject
myObject.value = 89;

// Print the updated state of myObject
console.log(myObject);

